server:
  port: 8080

spring:
  jpa:
    hibernate:
      ddl-auto: create-drop

    properties:
      hibernate:
        dialect: org.dialect.MySQL57Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/testdb
    username: root
    password: